PACKAGE = "cartesian_impedance_controller"
    
from dynamic_reconfigure.parameter_generator_catkin import *

gen = ParameterGenerator()



gen.add("f_x", double_t, 0, "Apply a force in x-direction",    0, -10,   10)
gen.add("f_y", double_t, 0, "Apply a force in y-direction",    0, -10,   10)
gen.add("f_z", double_t, 0, "Apply a force in z-direction",    0, -10,   10)
gen.add("tau_x", double_t, 0, "Apply a torque  around the x direction at the TCP",    0, -10,   10)
gen.add("tau_y", double_t, 0, "Apply a torque  around the y direction at the TCP",    0, -10,   10)
gen.add("tau_z", double_t, 0, "Apply a torqye  around the z direction at the TCP",    0, -10,   10)

gen.add("apply_wrench", bool_t, 0, "Apply the wrench by setting the value to true.",   False)

exit(gen.generate(PACKAGE, "cartesian_impedance_controller", "wrench_config"))
